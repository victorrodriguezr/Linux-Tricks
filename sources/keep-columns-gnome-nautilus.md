# Keep visible columns that you want on Nautilus folder manager:

```bash
gsettings set org.gnome.nautilus.list-view default-visible-columns "['name', 'size', 'owner', 'type', 'date_modified_with_time', 'detailed_type']"
```